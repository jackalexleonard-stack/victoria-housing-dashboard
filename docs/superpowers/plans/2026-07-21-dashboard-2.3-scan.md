# Dashboard 2.3 — "the scan" batch (design-review P0s + P1s)

> Executed subagent-driven (implement → adversarial review → fix), on `main`. User selected **P0s + P1s** from the board report — the authoritative per-item spec is `docs/superpowers/reviews/2026-07-21-design-review.md`; this plan only groups items into build tasks and fixes interfaces. The biggest UI change since launch: goal = the daily scan works again (target ≈40–50% shorter default mobile scroll, a finding sentence above the fold, nothing deleted).

## Task groups (one writer at a time; every task reviewed)

**T1 — pipeline & export (Python).** From the report: mixed-scale chart splits (People → NOM+natural only, ERP becomes a stat tile; credit → yoy trio default; Accord → cumulative-only card) [P0-5]; World synthesized finding rule + hero-eligibility down-weight of World series [P1-World]; per-metric display-label map exported (kills "credit housing mom") [P1-labels]; finding-copy fixes (align held-threshold to display precision; "is"→"was" on level-only) [P1-copy]; section-summary findings for collapsed rows ("World — iron ore extends its slide") [feeds P0-3]; lead-pick field so Today knows which hero pick leads [feeds P0-1]. Regenerate fixtures once at the end of the batch, not per task.

**T2 — chart internals (chartMath + LineChart).** Y-domains to data extent + padding for line marks, keep zero baseline only when data spans it; min-span guard; fix HVI top-clipping [P1-axes]. Annotation picket fence → single pale-clay cycle band on lending/credit/HVI, cash-rate card fence dropped (latest-move label only); density-aware labels everywhere; full labelled set modal-only; thin solid clay not dashed [P0-4]. De-emphasis gray treatment for 6-line tangles (finding's series gets its hue, others #878580 reduced weight; gray freed from data duty on mortgage chart; mortgage → new/outstanding small-multiple pair) [P1-emphasis].

**T3 — Today rework.** Lead-finding card (Newsreader, from lead pick) + two secondary finding cards + compact tile strip (pins prominent, sentence-case labels, deltas inline) [P0-1]; change strip renders deltas via TILE_FMT + delta_color, ranked, top ~6 with "and N more" disclosure; stale chips badged with vintage inline [P0-2, P1-stale]. Acceptance: one full finding sentence visible at scroll 0 at 393px (new e2e).

**T4 — sections & cards (App + ChartCard).** Desktop: World default-collapsed; News truncated to top stories + "Show all N". Mobile (coarse pointer): all post-Today sections default collapsed. Collapsed rows show the T1 section-summary line + worst status chip; persistence = per-section explicit overrides vs viewport default; deep links/chips auto-expand [P0-3]. Per-card metadata → ONE caption line (name · short source token · status chip · table toggle); notes+full source title move to the modal (add notes there first); legends inline-labelled when ≤3 series [P1-metadata]. Outage handling: shared-source section notice (Rents), dead Prices cards → compact stubs, honest per-series body line [P1-outage]. Masthead "N sources unavailable" becomes a keyboard-operable popover listing them [P1-stale].

**T5 — touch & polish-adjacent.** 44px targets: Filters/Done bordered buttons, sheet segments, chip hit-areas via padding, table toggles [P1-touch].

**T6 — verify & ship.** Regenerate fixtures from one export; new/updated e2e (fold-finding assertion, collapse defaults desktop+mobile, News truncation, popover); full suites; deploy; live verify (page-height metrics re-captured and compared: expect mobile default scroll materially down); board-report cross-check that every selected item landed or is explicitly deferred with reason.

Out of scope (deferred): the P2 polish bundle; anything the identity defender adjusted stays per its adjusted wording. Order: T1 → T2 → T3 → T4 → T5 → T6 (T2 may run parallel to T1 reviews only if no file overlap — both commit, so serialize writers as usual).
