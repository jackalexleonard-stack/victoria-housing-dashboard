# Dashboard 2.4 — polish bundle (re-verified P2 + World tile row + backlog sweep)

> Executed subagent-driven (implement → adversarial review → fix), on `main`. Authoritative per-item evidence: `.superpowers/sdd/p2-reverification.md` (2026-07-22 audit of the live 2.3 build — implement VERIFIED reality, not the original board wording). Board reference: the [P2] entry of `docs/superpowers/reviews/2026-07-21-design-review.md`. Excluded deliberately: Node-20 action bumps (deploy workflow stays untouched in a polish batch).

## D1 — charts & tiles
- **(b) Tick threshold:** compact rule applies to every step ≥1,000 (live break: oo_lending "5,000/10k/15k/20k"). chartMath formatter + tests.
- **(c) Greenfield stat tile:** `vic_land` carries 1 pt/metric → zero-length paths (blank plot). Convert the card to a stat-tile presentation (value + delta + staleness, no plot) until ≥2 pts/metric; auto-reverts to a chart when history accrues (data-driven gate, tested both sides).
- **(f) World KPI tile row (the partial P1):** expanded World renders one compact tile row (value + delta + staleness chip per series, tap → existing detail modal) instead of six full-height charts; the T1-exported World summary leads the section. Every series keeps full provenance one tap away. Collapsed-by-default behaviour unchanged.

## D2 — naming, layout, controls
- **(d) Naming & formatting:** canonical short-name per series used verbatim by tile / finding subject / caption (kills the Cotality triple + the doubled caption line); per-series source strings for the three FRED cards (modal + caption); signed currency renders "−$2,400" never "$-2,400" (formatter + stat-block paths, tested with negative values).
- **(e) Trailing cards:** sections whose grid ends on a dangling cell (Money 4, World 6 pre-D1 — recheck post-D1) span the last card full width.
- **(a, narrowed):** scrollspy chips restyled from pill to underlined text-tab form (aria semantics unchanged); pale-tint pills become exclusively status/provenance. Range/geo already done by 2.3 — do not touch.

## D3 — backlog sweep (10 items, two commits: mechanical / needs-tests)
Mechanical: dead dffh helpers removed (+their orphaned test updated); NewsSection/TodaySection caption helper extracted; DetailView/ChartCard shared derivation hook extracted (~15-20 lines); superseded '580.00%' assertion deleted; pandas resample DeprecationWarning fixed (explicit unit).
Needs-tests: explainer title= attrs → accessible pattern (aria-describedby or visible caption — keyboard reachable); guard-level health-less load test; TILE_CHART py/ts parity test (fixture-driven); World quiet-flag refactor (_world_summary returns the boolean); _hvi v==0 → display-precision threshold (align with the P1-copy fix class).

## Ship
Full suites → deploy → live spot-checks (tick fix on lending, World tile row, greenfield tile, "−$" sign) → ledger/memory. No re-measure needed (no scroll-structure change; World-expanded shrinks as a bonus — note the number informally).

Order: D1 → D2 → D3 → ship. One writer at a time; every task reviewed.
